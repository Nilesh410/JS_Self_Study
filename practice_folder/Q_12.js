function getSummary(one,two,three)
{
    console.log(one)
    console.log(two)
    console.log(three)
}
const fname="Nilesh"
//const age=34

getSummary `${fname} age is ${age}`//calling function with firt parameter as a array of string without variable value 
//and remaining prameters are replace with variable values